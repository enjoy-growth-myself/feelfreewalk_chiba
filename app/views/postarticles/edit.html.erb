<h2>投稿編集画面</h2>


	<h1>編集</h1>
		
      <div>
		<%= form_with(model: @postarticle, local: true) do |form| %>

	        <h4>タイトル</h4>
	        <%= form.label :title %>
	        <%= form.text_field :title %></p>
	        
	        <h4>内容</h4>
	        <%= form.label :body %>
	        <%= form.text_area :body %>

	        <h4>住所</h4>
	        <%= form.label :address %>
	        <%= form.text_field :address %>

	      	<h4>subumitボタン確認</h4>
	        <%= form.submit "更新", class: "btn btn-primary pull-right" %>
	      </div>
	   		<%= link_to "詳細画面に戻る", postarticle_path, class: "back" %>
	   	
    	<% end %>
    </div>
</div>

